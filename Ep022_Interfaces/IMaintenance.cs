namespace Ep022_Interfaces
{
    // two methods have the same signature in two different class
    // So, while implementing them we have to do it explicitly
    interface IMaintenance
    {
        void Maintenance();
    }
}
